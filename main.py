# Archivo principal que inicia la aplicación, importando la clase App desde el módulo interfaz y ejecutando el bucle principal de la aplicación para mostrar la interfaz gráfica al usuario.
from interfaz import App
if __name__ == "__main__":
    app = App()
    app.mainloop()
